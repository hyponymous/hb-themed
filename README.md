# A Harp-based Blog

A blog template using [harp](http://harpjs.com/) and [browserify](http://browserify.org/).

# Getting Started

In the root directory run

```
npm install
npm run watch
```

and open http://localhost:9001/ in a browser.

